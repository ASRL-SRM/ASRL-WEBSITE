import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  base: 'asrlwebsiteasrl.in-2025aimhighflyhigher', // 🔁 Replace with your repo name if it's different
  plugins: [react()],
})